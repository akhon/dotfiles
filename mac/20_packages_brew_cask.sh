#!/usr/bin/env bash

brew install balenaetcher --cask
brew install chatgpt --cask
brew install docker-desktop --cask
brew install iterm2 --cask
brew install tsh --cask
brew install usr-sse2-rdm --cask
